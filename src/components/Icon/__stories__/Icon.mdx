import { Canvas, Meta } from '@storybook/blocks'
import * as IconStories from './Icon.stories'

<Meta of={IconStories} title="components/Icon"/>

# Icon

A Icon is the box at the top of every screen. It includes the logo, the
software version, language toggle and an optional button to log out.

## Desktop

<Canvas of={IconStories.Primary} />
