import { Canvas, Meta } from '@storybook/blocks'
import * as DropFileStories from './DropFile.stories'

<Meta of={DropFileStories} title="components/DropFile"/>

# DropFile

A DropFile is the box at the top of every screen. It includes the logo, the
software version, language toggle and an optional button to log out.

## Desktop

<Canvas of={DropFileStories.BasicTemplate} />

