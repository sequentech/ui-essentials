import { Canvas, Meta } from '@storybook/blocks'
import * as LanguageMenuStories from './LanguageMenu.stories'

<Meta of={LanguageMenuStories} title="components/LanguageMenu"/>

# LanguageMenu

A LanguageMenu is the box at the top of every screen. It includes the logo, the
software version, language toggle and an optional button to log out.

## Desktop

<Canvas of={LanguageMenuStories.CollapsedMenu} />
